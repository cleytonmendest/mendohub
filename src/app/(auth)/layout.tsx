/**
 * Auth Layout
 *
 * Layout simples para páginas de autenticação (login, signup, etc).
 */

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
