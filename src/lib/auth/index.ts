/**
 * Auth - Barrel Export
 *
 * Exporta todos os helpers de autenticação em um único lugar.
 *
 * Uso:
 * import { getCurrentUser, requireAuth, requirePlatformAdmin } from '@/lib/auth';
 */

export * from './session';
export * from './guards';
